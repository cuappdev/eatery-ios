query BrbInfo($accountId: String) {
    accountInfo(id: $accountId) {
        cityBucks
        laundry
        brbs
        swipes
        history {
            amount
            name
            timestamp
            positive
        }
    }
}

